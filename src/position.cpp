#include "position.h" // Include the header file for the Position class

// Constructor for the Position class
// Initializes the row and column attributes with the provided values
Position::Position(int row, int column)
{
    this->row = row;       // Set the row attribute to the provided row value
    this->column = column; // Set the column attribute to the provided column value
}
